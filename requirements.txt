# This file is used for defining the direct dependencies of the project.
# For reproducible builds, the full list of pinned dependencies is in `requirements.lock`.
# To install all dependencies, run:
# pip install -r requirements.txt

ctransformers==0.2.27
optimum[onnxruntime]

-r requirements.lock
